# Ralph Progress Log
Started: Fri Mar 13 04:15:53 EDT 2026
---

## Codebase Patterns
- LangChain packages are hoisted to root node_modules (monorepo); imports resolve from there
- apps/web/.gitignore has `.env*` — must add `!.env.example` to track example env files
- Use `createReactAgent` from `@langchain/langgraph/prebuilt` (not from `langchain` package directly)
- `createReactAgent` param is `llm` (not `model`), accepts `checkpointer` directly
- Supabase server client uses cookies() — do NOT use inside LangChain tools (use JWT-initialized client instead)
- Use `tool()` from `@langchain/core/tools` (not `DynamicStructuredTool`) — it passes `RunnableConfig` as 2nd arg to func
- Access userId/supabaseClient via `config.configurable` in tool functions
- check_ins table column is `check_in_type` (not `type`), no `reflection` column exists

---

## 2026-03-13 - US-001
- Installed langchain, @langchain/core, @langchain/anthropic, @langchain/langgraph, @langchain/textsplitters
- Created apps/web/src/lib/langchain/config.ts with ChatAnthropic (claude-sonnet-4-5, temp 0.7, maxTokens 2000) and createMentorAgent() factory
- Created apps/web/src/lib/langchain/index.ts barrel export
- Created apps/web/.env.example with ANTHROPIC_API_KEY
- Updated .gitignore to allow .env.example
- Files changed: apps/web/package.json, apps/web/.gitignore, apps/web/.env.example, apps/web/src/lib/langchain/config.ts, apps/web/src/lib/langchain/index.ts
- **Learnings for future iterations:**
  - `createReactAgent` uses `llm` not `model` as the parameter name
  - The monorepo hoists @langchain packages to root node_modules
  - .gitignore needed `!.env.example` exception for the env example file
  - Typecheck and lint both pass cleanly — no new warnings introduced
---

## 2026-03-13 - US-002
- Created apps/web/src/lib/langchain/utils.ts with getSupabaseClient() and getUserId() helpers
- Created apps/web/src/lib/langchain/tools.ts with 5 tools: get_career_goals, get_progress_summary, get_recent_checkins, search_user_context (stub), get_career_ladder
- Updated apps/web/src/lib/langchain/index.ts barrel export with mentorTools, getSupabaseClient, getUserId
- All tools use `tool()` from @langchain/core/tools with Zod schemas
- All tools extract supabaseClient and userId from config.configurable (DRY via utils.ts)
- get_career_ladder does fuzzy ILIKE match, falls back to Generic FAANG/Startup template
- search_user_context returns stub message for future RAG wiring
- Files changed: apps/web/src/lib/langchain/utils.ts (new), apps/web/src/lib/langchain/tools.ts (new), apps/web/src/lib/langchain/index.ts
- **Learnings for future iterations:**
  - `DynamicStructuredTool` func receives `(input, runManager?)` — runManager is NOT RunnableConfig. Use `tool()` helper instead which passes RunnableConfig as 2nd arg.
  - `config.configurable` is typed loosely — cast through `Record<string, unknown>` for type-safe access
  - check_ins table uses `check_in_type` column (not `type`) and has no `reflection` column
  - ESLint runs via `npx eslint <path>` (not `next lint --dir`)
---
